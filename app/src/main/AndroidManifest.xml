<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="kr.ac.skuniv.for_impairment_project">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.For_impairment_project"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".activity.LoginActivity"
            android:label="@string/app_name"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.JoinActivity" android:exported="false"/>
        <activity android:name=".activity.ProtectorActivity" android:exported="false"/>
        <activity android:name=".activity.ProtectorNowActivity" android:exported="false"/>
        <activity android:name=".activity.ProtectorPastMoveActivity" android:exported="false"/>

    </application>

</manifest>